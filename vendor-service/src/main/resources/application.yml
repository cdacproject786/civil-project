server:
  port: 7575
spring:
  application:
    name: VENDOR-SERVICE
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:4545
  datasource:
      url: jdbc:mysql://localhost:3306/vendor
